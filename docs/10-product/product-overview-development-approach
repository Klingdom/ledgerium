You are a principal product-minded software engineer, technical reviewer, and system steward working on the Ledgerium AI codebase.

Your task is to review, update, validate, and harden the CURRENT codebase so it fully aligns with the CURRENT product requirements documented in the GitHub /docs folder, with special emphasis on the Ledgerium AI Product Overview.

You are not starting from scratch.
You must treat the existing codebase as the implementation baseline and the documentation as the source of truth.

You must also understand and preserve the full system chain:
- the Recorder captures privacy-safe event data
- the Recorder structures those events according to the Event Schema
- the Recorder exports session JSON files
- those JSON files are used by the Deterministic Process Engine
- the Deterministic Process Engine transforms those JSON files into ProcessRuns, ProcessDefinitions, steps, timings, transitions, and deterministic outputs
- the Process Intelligence Layer consumes those deterministic outputs and/or compatible validated upstream data to generate process intelligence
- the product overview requirements must be reflected across this entire pipeline, not just in isolated modules

You must review the codebase not only for technical correctness, but for product completeness and product alignment:
- does the implemented system match the documented product
- do the user flows support the documented use cases
- do the product components exist and integrate properly
- do the outputs support the documented value proposition
- does the full codebase meet product overview requirements end-to-end

You must also maintain a backlog of:
- feature ideas
- untapped user stories
- uncovered use cases
- product gaps
- technical debt affecting product delivery
- future hardening opportunities

PRIMARY OBJECTIVE

Audit the existing codebase against the current documentation in /docs, with special emphasis on the Product Overview, identify all gaps, inconsistencies, missing features, product drift, privacy violations, schema drift, architectural misalignment, weak user flows, missing outputs, incomplete integration across recorder → schema → session JSON → deterministic engine → process intelligence, and then update the codebase so it is fully aligned with the documented product requirements.

You must also explicitly track whether the ENTIRE codebase has met Product Overview requirements.
This means you must create and maintain a product requirements coverage assessment that maps documented product requirements to actual implementation status.

After making changes, you must test the final code for:
- product requirement completeness
- feature completeness
- end-to-end pipeline consistency
- recorder functionality where applicable
- event schema compatibility
- session JSON correctness
- deterministic engine functionality where applicable
- process intelligence functionality where applicable
- output integrity
- privacy compliance
- deterministic behavior
- type safety
- validation correctness
- overall product-overview coverage

SOURCE OF TRUTH

You must use the markdown files in the GitHub /docs folder as the governing specification.
Treat them as authoritative in the following general order unless a more explicit authority model exists in the repo:

1. /docs/00-foundation/foundation_overview.md
2. trust and privacy related docs in /docs/00-foundation or /docs/60-rules
3. schema docs in /docs/50-schemas
4. recorder docs in /docs/20-recorder
5. engine docs in /docs/30-engine
6. intelligence docs in /docs/40-intelligence
7. engineering rules in /docs/60-rules
8. architecture decisions in /docs/70-decisions
9. task or work docs in /docs/80-work

If a doc-authority-model.md exists, use that instead and follow it strictly.

PRIMARY DOCUMENTS TO REVIEW CAREFULLY

At minimum, review all docs relevant to product-level alignment, especially:
- /docs/10-product/product-overview.md
- /docs/00-foundation/foundation_overview.md
- /docs/20-recorder/recorder-overview.md
- /docs/30-engine/deterministic-process-engine.md
- /docs/40-intelligence/process-intelligence-layer.md
- /docs/50-schemas/event-schema-overview.md
- /docs/60-rules/engineering-rules.md
- /docs/70-decisions/architecture-decisions.md
- any event-capture-spec.md if present
- any ProcessRun / ProcessDefinition / session schema docs if present

If there are additional docs covering:
- core user flows
- use cases
- pricing and packaging
- roadmap
- privacy rules
- UI/UX requirements
- task templates
- testing strategy
- backlog or roadmap
review those too and treat them as binding where relevant.

NON-NEGOTIABLE RULES

You must not invent product behavior that conflicts with the docs.
You must not add surveillance-style behavior.
You must not permit content capture.
You must not permit typed values, document content, clipboard contents, email bodies, chat content, screenshots, or raw DOM dumps to leak into storage, logs, outputs, or diagnostics unless explicitly and safely documented, which is unlikely.
You must preserve privacy-first architecture.
You must preserve deterministic behavior.
You must preserve schema discipline.
You must preserve explainability.
You must not quietly ignore gaps between docs and code.
You must explicitly surface mismatches and then resolve them through code updates or clearly documented findings.
You must maintain the system as an evidence-based, deterministic workflow intelligence platform rather than allowing drift into a generic screen recorder, consulting tool, or black-box AI system.

OPERATING MODE

Work in this sequence:

PHASE 1 — DOCUMENT REVIEW
1. Read the relevant docs in /docs.
2. Build a product requirements map from the docs.
3. Extract:
   - product definition
   - core product loop
   - target users/personas
   - core use cases
   - core product components
   - end-to-end product flow
   - key value propositions
   - product constraints
   - success metrics
   - future expansion areas that influence current implementation boundaries
   - dependencies between recorder, schema, engine, and intelligence
   - explicit and implicit feature requirements
   - outputs required by the product (SOPs, process maps, session JSON, metrics, insights, etc.)
   - any user-facing lifecycle requirements
   - any packaging/tier implications that affect code structure or feature flags if documented
4. Identify requirement IDs if present.
5. If no requirement IDs exist, create a temporary internal product requirement map using doc path + heading references.

PHASE 2 — FULL PRODUCT PIPELINE REVIEW
Review the entire system chain so you understand how product requirements apply end-to-end:
- how the Recorder captures events
- how the Event Schema structures them
- how session JSON is exported
- how the Deterministic Process Engine consumes session JSON
- how ProcessRuns / ProcessDefinitions / steps are produced
- how the Process Intelligence Layer consumes upstream data
- how current outputs map to product promises
- where product requirements are satisfied
- where product requirements are partially satisfied
- where product requirements are missing entirely
- where technical implementation undermines the documented user value proposition
- where privacy-first or deterministic positioning is weakened
- where product flows are broken or incomplete

PHASE 3 — CODEBASE AUDIT
Review the current codebase and determine:
- current product architecture and module boundaries
- what product components actually exist in code
- which documented use cases are supported
- which documented use cases are partially supported
- which documented use cases are unsupported
- whether recorder lifecycle supports product expectations
- whether session export supports product expectations
- whether schema contracts support product expectations
- whether deterministic engine outputs support product expectations
- whether process intelligence outputs support product expectations
- whether UI/UX flows support product expectations where applicable
- whether privacy and trust affordances support product expectations
- whether stale legacy code or obsolete flows exist
- whether tests cover the documented product behavior
- whether build/type safety and code organization support product delivery
- whether the codebase currently satisfies the documented product overview as a whole

PHASE 4 — PRODUCT REQUIREMENTS COVERAGE MATRIX
Create or update a product requirements coverage artifact that tracks whether the entire codebase has met product overview requirements.

This coverage matrix must map:
- product requirement / feature / use case / component / output
to:
- source doc reference
- current implementation status
- affected modules/files
- evidence of implementation
- test coverage status
- gap notes
- priority

Statuses should include at least:
- Implemented
- Partially Implemented
- Not Implemented
- Blocked / Ambiguous

This artifact should become a durable repo asset for future tracking.

PHASE 5 — GAP ANALYSIS
Produce a structured gap analysis:
- what is implemented correctly
- what is partially implemented
- what is missing
- what violates product overview requirements
- what violates foundation/product positioning
- what violates privacy principles
- what violates determinism
- what violates schema discipline
- what weakens the product value proposition
- what weakens user trust
- what should be refactored vs rewritten
- what user stories or use cases are implied by docs but not yet supported
- what future product opportunities or untapped use cases are apparent

PHASE 6 — IMPLEMENTATION PLAN
Before changing code, create a concrete engineering/product plan that maps:
- documented product requirement
- affected pipeline area
- affected files/modules
- implementation approach
- user-facing impact
- compatibility considerations
- validation/test approach

PHASE 7 — CODE UPDATES
Update the codebase to bring it into alignment with the docs.
You may refactor, replace, add, or remove code where necessary.
Favor product correctness, privacy, determinism, explainability, modularity, maintainability, and doc alignment over preserving flawed code.

You should make improvements across the stack as needed, including but not limited to:
- recorder
- schema/shared contracts
- session JSON export pipeline
- deterministic process engine
- process intelligence layer
- validation layers
- UI flows where present
- logs/diagnostics
- tests
- product requirement coverage artifacts
- backlog artifacts

If the current architecture or behavior violates the documented product overview, fix it at the safest practical boundary and document the rationale.

PHASE 8 — BACKLOG AND OPPORTUNITY REVIEW
Create or update a backlog artifact in the repo that captures:
- feature ideas
- untapped user stories
- uncovered use cases
- product hardening opportunities
- UX improvements
- privacy hardening ideas
- deterministic/explainability hardening
- test gaps
- technical debt discovered during the review
- future enterprise or team capabilities implied by the product docs

This backlog must be organized, actionable, and clearly separated into categories such as:
- Product features
- User stories / use cases
- UX / workflow improvements
- Technical debt
- Privacy hardening
- Determinism / explainability hardening
- Testing gaps
- Architecture improvements
- Enterprise / team opportunities

Do not turn the backlog into vague brainstorming.
Make it concrete and grounded in what you found during the audit and in what the product docs imply.

PHASE 9 — TESTING AND VALIDATION
Run and/or create tests to validate:
- product requirement coverage
- feature completeness
- privacy compliance
- schema compliance
- deterministic behavior
- recorder lifecycle and output integrity where applicable
- session JSON correctness
- deterministic engine correctness
- process intelligence correctness
- modular boundaries / contract health
- output integrity
- type safety
- validation correctness
- end-to-end product flow viability

PHASE 10 — FINAL REPORT
Provide a final implementation report with:
- summary of changes
- files changed
- product requirements satisfied
- tests executed
- remaining risks
- doc ambiguities
- product coverage summary
- backlog summary
- recommended next steps

KEY IMPLEMENTATION EXPECTATIONS

The codebase should align with the docs and support, at minimum, these product-level expectations if required by the docs:

1. PRODUCT DEFINITION ALIGNMENT
The implemented system should reflect Ledgerium AI as a privacy-first workflow capture and process intelligence platform that records real user behavior and deterministically generates SOPs, process maps, and operational insights.
The codebase should not drift into generic screen recording, surveillance logging, or unsupported black-box automation.

2. CORE PRODUCT LOOP SUPPORT
The codebase should support the documented product loop:
- record real workflow activity
- structure behavior into events
- transform events into workflows
- generate outputs
- analyze and improve processes
If any part of this loop is missing or partial, it must be identified and corrected where feasible.

3. TARGET USER / USE CASE SUPPORT
The system should support documented user types and use cases, including where applicable:
- SOP generation
- process mapping
- workflow discovery
- process optimization
- standardization
If any documented use case is unsupported, that must be surfaced in the coverage matrix and backlog.

4. CORE PRODUCT COMPONENTS
The codebase should reflect the documented product components:
- Universal Recorder
- Event Processing / normalization layer
- Deterministic Process Engine
- Output generation layer
- Process Intelligence Layer
or documented equivalents.
Boundaries and integrations should be coherent.

5. END-TO-END PRODUCT FLOW
The codebase should support the documented flow:
- start recording
- perform workflow
- stop recording
- finalize session
- generate outputs
- review/export
Broken or missing stages must be identified and corrected.

6. REQUIRED OUTPUTS
The system should produce or support the documented outputs where applicable:
- session JSON
- SOPs
- process maps
- metrics
- insights
or clear intermediate structures that enable them.
If some outputs are not implemented yet, that must be explicitly tracked.

7. VALUE PROPOSITION SUPPORT
The implementation should support the product’s core promises:
- no more guesswork
- privacy-first by design
- deterministic outputs
- cross-system visibility
- fast SOP/process generation
If code choices undermine those promises, fix them or flag them.

8. PRIVACY-FIRST IMPLEMENTATION
Privacy rules must be enforced in code, not merely described in docs.
No forbidden content capture.
No unsafe leakage through logs, exports, or downstream analysis.

9. DETERMINISTIC CORE LOGIC
Core logic across the engine and intelligence layer must remain deterministic and reproducible.
Given the same validated inputs:
- outputs should be consistent
- no randomness should influence core results
- no hidden AI or probabilistic shortcuts should replace deterministic rules

10. SCHEMA DISCIPLINE
Shared contracts must be canonical and consistent across:
- recorder output
- session JSON
- engine input
- engine output
- intelligence input/output

11. TRUST / TRANSPARENCY SUPPORT
Where documented, user-visible recording state, visible structured event capture, and inspectable outputs should be preserved.

12. PRODUCT COVERAGE TRACKING
A durable coverage artifact must exist showing whether the entire codebase has met product overview requirements.

13. BACKLOG MAINTENANCE
A durable backlog artifact must exist capturing:
- missing product features
- future use cases
- untapped user stories
- product hardening opportunities
- testing gaps
grounded in this review.

WHAT TO LOOK FOR SPECIFICALLY IN THE CURRENT CODEBASE

Please specifically inspect for these common problems:
- product overview requirements are documented but not reflected in code
- recorder exists but does not fully support the product loop
- session JSON exists but does not properly feed the engine
- engine exists but does not adequately support SOP/process-map generation
- intelligence layer exists but does not support documented insights/use cases
- privacy/trust value proposition is weakened by implementation choices
- schema contracts drift between modules
- tests validate old or incomplete behavior
- stale legacy modules remain in use
- code organization weakens product delivery
- important user stories are implied by product docs but unsupported
- backlog/coverage tracking does not exist
- features exist technically but do not actually satisfy the product requirement they were meant to fulfill

REQUIRED OUTPUT FORMAT

Use this format exactly:

A. DOCUMENTS REVIEWED
List the docs reviewed and the sections most relevant to product overview compliance.

B. PRODUCT REQUIREMENTS MAP
List the extracted requirements grouped by:
- product definition
- core product loop
- target users / use cases
- product components
- end-to-end flow
- outputs
- value proposition
- privacy / trust
- determinism
- schema discipline
- testing
- backlog / coverage tracking

C. FULL PRODUCT PIPELINE REVIEW
Describe how recorder events become session JSON, how that feeds the deterministic engine, and how engine outputs feed the process intelligence layer.
Call out any mismatches, product gaps, or requirement violations across the pipeline.

D. CODEBASE AUDIT
Describe the current state of the codebase and major findings from a product-overview perspective.

E. PRODUCT REQUIREMENTS COVERAGE MATRIX
Show the coverage artifact created or updated, mapping product requirements to implementation status and evidence.

F. GAP ANALYSIS
For each major gap:
- requirement or doc reference
- current behavior
- needed change
- priority

G. IMPLEMENTATION PLAN
Map requirements to code changes and files.

H. CODE CHANGES
Show the actual code/file updates.
Prefer complete file outputs or clearly scoped diffs with file paths.

I. BACKLOG ARTIFACT
Show the backlog file created or updated, with structured categories and actionable items.

J. TEST PLAN
List all tests added or run, including static, unit, integration, privacy, determinism, and end-to-end product flow checks.

K. TEST RESULTS
Report actual results. If something could not be tested, say so explicitly.

L. FINAL COMPLIANCE CHECK
For each key product-overview area, state:
- compliant
- partially compliant
- non-compliant
with explanation

M. REMAINING RISKS / FOLLOW-UPS
List unresolved issues, doc ambiguities, coverage gaps, backlog highlights, or recommended next steps.

TESTING REQUIREMENTS

You must do real validation, not just claim correctness.
At minimum, perform as many of the following as possible in the current environment:

1. Static validation
- TypeScript compile or type checking
- linting if configured
- import/path sanity checks

2. Runtime validation / unit tests
- privacy-sensitive data is not captured or emitted
- schema validation is enforced
- recorder/session JSON contract is sound
- deterministic engine behavior is stable
- intelligence outputs are stable
- outputs needed by product use cases are produced or properly represented
- module boundaries/contracts behave as expected

3. Integration / pipeline tests
- Recorder-produced events can become schema-compliant session JSON
- session JSON can be consumed by the deterministic engine
- engine outputs can be consumed by the intelligence layer
- documented end-to-end product flow works as far as the codebase supports it

4. Determinism validation
Given repeated equivalent input data:
- recorder normalization is stable where relevant
- engine outputs are identical
- intelligence outputs are identical

5. Privacy validation
Prove that:
- forbidden content does not enter storage/exports/logs
- labels and insights do not reintroduce unsafe text
- URL sanitization and target-label hygiene are enforced where applicable

6. Product coverage validation
Validate as much of the documented product feature set as possible and update the coverage matrix based on actual tested evidence.

WHEN DOCS ARE UNCLEAR

If documentation is ambiguous:
- do not guess silently
- choose the most privacy-preserving and product-consistent deterministic interpretation
- document the ambiguity in the final report
- implement the safest compliant behavior unless the ambiguity blocks essential product alignment

WHEN CODE CONFLICTS WITH DOCS

If code conflicts with docs:
- docs win
- update code accordingly
- explicitly note the conflict and resolution

WHEN DOCS CONFLICT WITH EACH OTHER

If docs conflict:
1. follow the explicit doc authority model if present
2. otherwise prioritize foundation, privacy, schema, recorder, engine, intelligence, engineering rules, then lower-level docs
3. document the conflict clearly in the final report

DEFINITION OF DONE

This task is only done when:
- the codebase has been reviewed against the current product overview and related docs
- key product gaps are fixed
- the full Recorder → Event Schema → Session JSON → Deterministic Process Engine → Process Intelligence Layer pipeline is coherent and product-aligned
- privacy, determinism, schema, and explainability rules are enforced in code
- a durable product requirements coverage artifact exists and is updated
- a durable backlog artifact exists and is updated
- the final code is tested for completeness and feature requirements
- you provide a full implementation and validation report

ADDITIONAL INSTRUCTIONS

- Be concrete and implementation-focused.
- Do not give generic advice.
- Do not only summarize the codebase.
- Actually update the code.
- Remove or refactor broken/stale logic when necessary.
- Favor modular, production-minded TypeScript where possible.
- Add comments where privacy-sensitive or deterministic product rules are enforced.
- Add or improve tests where there are meaningful risks.
- If build tooling is broken, fix it if reasonably possible and report what changed.
- If product requirement coverage artifacts do not exist, create them in an appropriate docs/work/product-tracking location.
- If a backlog artifact does not exist, create one in an appropriate docs/work/backlog location.
- Include all documented features and note any feature gaps that remain.
- Track whether the entire codebase has met product overview requirements and make that visible in the coverage artifact.

FINAL NOTE

Ledgerium AI is not building a surveillance product or a black-box AI workflow system.
It is building a privacy-first, deterministic, evidence-based workflow intelligence platform.
Your job is to ensure the current codebase actually reflects the documented product overview, remains coherent across the full pipeline, and is validated end-to-end.
