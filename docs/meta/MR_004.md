# Meta-Review 004 (MR-004)

**Date:** 2026-04-20
**Loops evaluated:** iter 014 → 017 (4 bounded loops) + Mode 3 pricing-audit @ iter 016→17 + Mode 4 MR-003 @ iter 015
**Status:** Complete
**Prior meta-review:** `C:\Users\philk\ledgerium\META_REVIEW_003.md` (at repo root, not under `docs/meta/`)

## Summary

The improvement loop is healthy on execution quality but structurally starved of discriminating selections. MR-003's governance diffs worked (Change A hygiene applied cleanly; Change B cool-off invoked once; Change D counter reset once), but the cool-off was consumed on a `directed` item (iter 016) rather than exercising the refined `top-score` formula — so the core MR-001 scoring rewrite is still validated on exactly 1 loop (iter 009) out of 17. Simultaneously, the follow-up pool grew 15 → 23 over 3 loops despite Change C's forcing, because the pricing-audit intake added 4 P0 items + 3 Mode-3 residuals + 1 PRD-promotion on a base where net closure/generation is running at ≤1. **Top-2 recommendations:** (1) narrow the cool-off clause 7 to exclude `directed` so the mechanism actually serves its designed purpose; (2) require explicit user acknowledgement (not "flag") before Mode 5 sequences of ≥4 items in a single Area, because Path B (iter 018–021) will spend 4 consecutive iterations on web-app with zero burn-down and zero refined-formula exercise, pushing MR-005 out to iter 022.

## Agenda Item 1 — Audit-intake pattern

- **Evidence:** `SYSTEM_HEALTH.md` L60 (`PRICING_AUDIT_001.md` present); `IMPROVEMENT_BACKLOG.md` L47–48 ("only P0 items entered live backlog. P1/P2/P3 items (≈27 additional) held as cold pool"); `ITERATION_LOG.md` L1307 ("Audit-intake pattern (new): P0-only live promotion + P1/P2/P3 cold pool"); `ITERATION_LOG.md` L1293–1298 lists the 4 P0 promotions (#33, #34, #35, #36).
- **Finding:** The pattern functioned as designed on P0 triage: 4 P0s entered live, ~27 P1/P2/P3 stayed cold, the pool-size ceiling signal was preserved (would have gone to ~42 otherwise — well above 8, but also well above the point where the ceiling rule remains diagnostically useful). P1/P2/P3 surfacing: only one promotion observed so far (BUG-07 via PRD trigger, Agenda 2). Insufficient evidence for false-negatives — we cannot yet tell whether any cold-pool P2 should have been P0 without waiting to see whether one bites. The Mode 3 commit closed all 3 P0 bugs in one iteration, so P0 classification accuracy is currently 3/3.
- **Recommendation:** **Keep, with one codification.** Add to `CLAUDE.md` a short § Audit-Intake Pattern section stating: (a) audit-style artifacts produce a cold-pool reference doc, (b) only P0 items enter the live backlog at intake time, (c) cold-pool items are tagged `audit-intake` with `Birth iter` = the audit-producing iteration, (d) P1/P2/P3 promote to live backlog one-at-a-time as P0s burn down OR on explicit PRD trigger (see Item 2). This pattern should generalize beyond pricing audits; it is the correct answer to any future "surface a large debt surface" event.

## Agenda Item 2 — PRD-trigger promotion pattern

- **Evidence:** `IMPROVEMENT_BACKLOG.md` L117 (row #40 marked "PROMOTED from audit cold pool on 2026-04-20"); L117 rationale: "hard blocker for approved `PRD_TEAM_TRIAL.md` (Dependency §11a)"; `SYSTEM_HEALTH.md` L77 ("BUG-07 blocks Team Trial build entry"); `IMPROVEMENT_BACKLOG.md` L147 ("governance rationale documented in audit § Governance Notes; MR-004 will evaluate the audit-intake + PRD-trigger promotion pattern").
- **Finding:** N=1 so far. But the trigger logic is defensible: a PRD approval creates a documented, artifact-backed dependency claim that is verifiable (a reader can open `PRD_TEAM_TRIAL.md` §11a and confirm BUG-07 is cited). That is stronger evidence than a coordinator judgment call. The pattern is at risk of abuse only if PRDs start citing cold-pool items they do not actually depend on — a governance problem, not a mechanism problem.
- **Recommendation:** **Formalize as a rule.** Propose for `CLAUDE.md`: *"Any item in the audit cold pool MAY be promoted to the live backlog if and only if a newly-approved PRD explicitly cites it as a hard blocker in a numbered Dependencies section. The promoting iteration's log entry must reference the PRD path and section number (e.g., `PRD_TEAM_TRIAL.md §11a`). The promoted row's `Birth iter` field is `PRD-promoted` with the promoting-PRD path noted. No promotion on implicit or narrative dependency — must be an enumerated, reader-verifiable citation."* This closes the abuse vector and preserves the audit cold pool's pool-size-ceiling protection.

## Agenda Item 3 — Staleness-cap triage

- **Evidence:** `IMPROVEMENT_BACKLOG.md` L91 (#14, Birth iter 007) and L92 (#15, Birth iter 006); `SYSTEM_HEALTH.md` L78 ("#15 (Birth 006, age 11 — 1 iter past cap) + #14 (Birth 007, age 10 — at cap)"). `IMPROVEMENT_BACKLOG.md` L84 (#7, Birth 008, reaches cap at iter 018 per L149); no other rows ≥10 iters old per scan.
- **Findings & recommendations per item:**
  - **#15 Extract confidence thresholds to shared constants** (score 10, age 11, Area = code hygiene, Effort 1 / Risk 1). **RECOMMEND: KEEP (preemptive close next eligible `burn-down` iter).** Evidence for keep: still has a concrete file address (`renderHelpers.ts ↔ sopTemplates.ts` circular); E=1/R=1 makes it a cheap closure that improves closure ratio without real cost. Not "delete" because it remains a correct defect; not "downgrade" because the score already reflects its low strategic weight. The only reason it is still open is ceiling-forced selection of higher-score items. Action: tag as "burn-down priority — staleness-cap override" so iter 022 (first post-Mode-5) selects it by default.
  - **#14 Wire `validateRenderedSOP` into `processSession.ts`** (score 11, age 10, Area = SOP quality gate, Effort 2 / Risk 2). **RECOMMEND: KEEP.** Evidence: it wires an already-built validator (iter 007) into the production pipeline — classic "last mile" work, high leverage per effort. SOP quality is a Phase-1 deliverable class that Phase 2 will depend on. Action: same as #15 — preemptive close priority at iter 022+.
  - **#7 Widen policy-engine `credit_card` regex** (score 11, age 9, reaches cap iter 018). **RECOMMEND: KEEP, flag for MR-005.** Very small patch (I=2 E=1 R=1), but policy-coverage Area with narrow test set, so safer to pair with adjacent policy work. No triage decision forced yet — just flag for MR-005's staleness scan.

## Agenda Item 4 — Web-app surface portfolio balance

- **Evidence:** `ITERATION_LOG.md` iter 014 (extension `ReviewScreen` + `HistoryDetailScreen` + `bundle-builder.test.ts` — extension-app); iter 015 (Mode 4 governance); iter 016 (`apps/web-app/src/app/(app)/dashboard/page.tsx` — web-app); iter 017 (`apps/web-app/src/app/api/billing/webhook/route.test.ts` + `feature-gating.test.ts` + `upgrade-button-error-state.spec.ts` — web-app); Mode 3 @ 016→17 (5+ web-app files). Upcoming Mode 5 Path B (iter 018–021) stated as 4× web-app.
- **Finding:** MR-003 Signal 5 was "web-app under-surveyed." Post-MR-003 swing: iter 016 + Mode 3 + iter 017 = 3 consecutive web-app touches; Path B will make that 7 consecutive. In the window iter 016–021, web-app will receive 6 of 6 bounded-loop + 1 Mode-3 = **7 touches vs extension 0, segmentation-engine 0, normalization-engine 0, policy-engine 0, process-engine 0**. Signal 5 over-corrected. MR-003 Change D's portfolio-drift trigger (10+ consecutive iterations without touching a tracked non-extension surface) is now mirrored in reverse and is NOT currently armed for the reverse direction. `SYSTEM_HEALTH.md` L98 correctly flags the ambiguity on whether test-only touches count — MR-004 should rule.
- **Recommendation:** **Rule explicitly on test-touch counting + add reverse-direction trigger.** (a) Test-only touches **count** toward surface coverage for Signal-5 purposes (they exercise determinism and catch regressions — that is the surface benefit the rule was designed to surface); this matches the Mode-3 intuition in `SYSTEM_HEALTH.md` L98. (b) Propose MR-003 Change D symmetry: add a second portfolio-drift early-trigger — *"10+ consecutive iterations without touching any tracked extension surface (extension-app, segmentation-engine, normalization-engine, policy-engine) flags reverse portfolio drift."* Counter starts from iter 014 (last extension touch); currently at 3 (iter 016 + 017 + iter 018 once it lands) and would reach 7 by iter 021. Armed but not yet triggered. MR-005 will have hard evidence either way.

## Agenda Item 5 — MR-002 Change A/B/C/D effectiveness

- **Change A (density-response log line):** Evidence: `ITERATION_LOG.md` L1006 (iter 014 "2 generated. Below the ≥3 threshold. `density-response:` log line not required"); L1172 (iter 016 "0 generated"); L1300 (Mode 3 "density-response: acknowledged, carried forward" explicitly logged with rationale); L1395 (iter 017 "1 follow-up generated — below the 3-item threshold"). **Verdict: WORKING.** Fired exactly once in 4 loops (Mode 3, correctly), structural-negative logged every other time. No silent violations. **No refinement needed.**
- **Change B (Birth iter column):** Evidence: `IMPROVEMENT_BACKLOG.md` rows #33 onward all carry Birth iter (`audit-intake`, `M3@016→17`, `PRD-promoted`, `017`). No unbackfilled rows. **Verdict: WORKING.** The audit-intake Mode 3 extended the anchor vocabulary cleanly (`audit-intake`, `PRD-promoted`) without schema violation. **No refinement needed.**
- **Change C (pool-size ceiling):** Evidence: `IMPROVEMENT_BACKLOG.md` L46 pool = 23 (was 15 pre-Mode-3); L144 "iter 018 MUST be burn-down unless cool-off invoked"; but iter 018 is now Mode 4 (this MR), and iter 019–021 are Mode 5 Path B (directed, not burn-down). **Verdict: STRUCTURAL RISK.** The ceiling rule is about to become inoperative for 4 consecutive loops — the directed Path B will bypass clause 6 via the `directed` selection rule (which is not ceiling-governed). The pool will almost certainly exceed 25 by iter 021. **Refinement needed** — see Proposed CLAUDE.md changes below: Mode 5 directed sequences of ≥3 items in the same Area should trigger a mandatory companion burn-down before the sequence can begin.
- **Change D (scope-expansion protocol):** Evidence: `ITERATION_LOG.md` L966 (iter 014 "None. No Mode 5 guardrail 7 invocation"); L1107 (iter 016 "scope-expansion: n/a"); L1335 (iter 017 "No scope-expansion protocol invocation"); L1309 (Mode 3 "no scope-expansion invocation required. Each engineer ... explicitly rejected adding new surface"). **Verdict: WORKING AS DETERRENT.** 4 consecutive opportunities where adjacent work could have been bundled; 0 invocations, and iter 016's dead-code narrowing explicitly cited as "scope-discipline signal, not a follow-up" (L1170). This is textbook Change D behavior. **No refinement needed.**

## Agenda Item 6 — MR-003 Change A/B effectiveness

- **Change A (CLAUDE.md hygiene refresh):** Evidence: `CLAUDE.md` § Current Phase now lists Phase 1 blockers closed + Phase 2 planning priorities (no stale `[BLOCKER]` rows); § Known Issues lists "No current Phase-1 release blockers" + follow-up pool count. **Verdict: WORKING.** Artifact cross-consistency achieved (`ITERATION_LOG.md` L1062 "all three files now agree"). No drift back observed. **No refinement needed.**
- **Change B (ceiling-cool-off clause 7):** Evidence: invoked at iter 016 on a `directed` item (`ITERATION_LOG.md` L1099), consumed, not yet re-armed (iter 017 burn-down #1 of next streak; `SYSTEM_HEALTH.md` L166). **Verdict: MIS-USED AS DESIGNED.** Clause 7's text does permit `directed` as a post-cool-off rule — but MR-003's explicit motivation (Signal 1: refined formula validated on only 1 loop) was to unlock `top-score` exercise. Consuming the single-use cool-off on a user-directed pick produces zero formula-validation evidence. The `directed` pick was the correct operational choice (CEO priority); the cool-off mechanism was the wrong tool to enable it, because `directed` does not actually need cool-off — a `directed` Mode 2/5 iteration can bypass clause 6 on operating-mode grounds alone. **Refinement needed:** narrow clause 7 to exclude `directed` (see Proposed CLAUDE.md changes). Pool-size post-iter-017: 23 (up from 15 at MR-003); cool-off did NOT drop pool, did NOT exercise formula — dual failure on its intended purposes. `top-score` autonomous ratio unchanged at **1/10** per `SYSTEM_HEALTH.md` L36.

## Agenda Item 7 — Agent diversity

- **Evidence:** `ITERATION_LOG.md` iter 014 = frontend-engineer (L964); iter 015 = Mode 4 meta-coordinator (L1033); iter 016 = frontend-engineer (L1112); iter 017 = backend-engineer (L1341); Mode 3 = backend+frontend parallel (L1237–1239).
- **Finding:** Rolling-5 bounded-loop primaries (iter 013–017, excl Mode-4): backend · frontend · frontend · [Mode 4 skip] · backend = 3 distinct primaries. Same-implementer-4+ trigger is 3 away (`SYSTEM_HEALTH.md` L160). No monopoly forming. However: the upcoming Mode 5 Path B will likely be frontend-engineer × 3 or 4 (dashboard redesign) — if it lands all 4 on frontend-engineer, iter 021 would put frontend-engineer at 3-of-5 (iter 016 + 018 + 019 + 020 + 021 = 5 in 10-loop window, but 4-in-5 consecutive) and trigger the same-implementer-4+ rule mid-sequence.
- **Recommendation:** **Keep rotation rule as-is** but **flag the Path B sequence** in the coordinator's Mode 5 brief that if 3 of the 4 items will use the same primary, the 4th item MUST use a different primary — forces mid-sequence diversity even under directed scope.

## Agenda Item 8 — Scoring formula discrimination

- **Evidence:** `IMPROVEMENT_BACKLOG.md` L80–119 full score distribution. Top scores: #33 = 12 (closed iter 017); #4 = 13 (top live); #5 = 12; #6 = 12; #10 = 11 (tied cluster); #7/8/9/14/19/31/36/40 all = 11; #11/12/13/15/20/24/26/30 = 10; #16/17/21/23/27/29 = 9; #28/32/38/41 = 7; #37/39 = 6.
- **Count within 2 points of live top (#4 @ 13):** items ≥ 11: 11 items (#4, #5, #6, #7, #8, #9, #10, #14, #19, #31, #36, #40). That is **12 items within 2 points** of the top (including #5 @ 12). Per the task's threshold ("If >10, scoring is under-powered"), **scoring is under-powered**.
- **Finding:** The refined formula (range 6–18) is theoretical; actual observed range is 6–13. The `release_blocker_bonus` +3 term cannot fire (no blockers open per `SYSTEM_HEALTH.md` L102–110), so an entire 3-point discriminating axis is dormant. The `saturation_penalty` −2 hasn't fired in 4 loops either (L30–31). This means the adjustments portion of the formula is contributing **zero** discriminating power right now. All current discrimination comes from the base `I+A+L+C−E−R` which ranges ~6–13 in practice.
- **Recommendation:** **Refine scoring to create Phase-2 discriminating terms.** Two options, both cheap: (a) **Strategic-alignment multiplier** — in Phase-2 planning, items citing a currently-approved PRD (e.g., Pro tier, Team trial) get `+2 PRD-bonus` per-iteration-while-PRD-is-active; this fires immediately for #40 (which would jump 11 → 13) and gives Phase-2 work measurable lift. (b) **Staleness-decay boost** — any follow-up at age ≥10 gets `+1 staleness-bonus`; fires for #14 and #15 (both jump to 12/11). Both terms are grep-able from existing artifact state. Recommend adopting (a) + (b) at MR-005 after observing one more cycle — not in MR-004 because changing scoring terms simultaneously with narrowing cool-off (Agenda 6) and Mode-5 companion-burn-down (Agenda 9) violates the "one control variable at a time" principle of the stability window.

## Agenda Item 9 — Mode 5 Path B governance preview

- **Evidence:** Mode 5 Path B is 4 items, all web-app dashboard redesign, iter 018–021. MR-005 cadence: Mode 5 counter increments by N (= 4) per `CLAUDE.md` § Mode 5 guardrail 1 + § Meta-Review Cadence increment rule. Base cadence = 3; next MR is therefore due at **iter 022** (iter 018 counts as MR-004 itself → Mode 5 iter 019=+1, 020=+2, 021=+3, MR-005 at 022). **Correction:** MR-004 = iter 018, which is a governance iteration; per `CLAUDE.md` § Operating Modes Mode 4 "does not count toward improvement-loop cadence." So the 3 Mode-5 items at iter 019/020/021 satisfy base cadence → MR-005 triggers at iter 022 boundary.
- **Governance risks identified:**
  1. **All 4 items same Area (web-app dashboard)** — would trigger 3-consecutive-same-Area early trigger at iter 021 if the rule counted Mode-5 items individually. `CLAUDE.md` § Mode 5 guardrail 6 only says "flag saturation risk," which is weaker than the early trigger's "forces meta-review."
  2. **Zero burn-down across 4 iterations** — pool at 23 start; with typical ~1 follow-up/iter generation and 0 closures, pool at iter 022 start = ~27. Ceiling rule diagnostic value degrades further.
  3. **MR-005 push-out + cool-off non-arming** — cool-off rearms after 3 consecutive `burn-down`-forced iterations. Iter 017 = #1; iter 019/020/021 = `directed` (not burn-down). Cool-off streak resets. First eligible cool-off would be iter 023+ IF iter 022+ are burn-down.
  4. **Zero extension-surface touch across 7 consecutive iterations** (iter 015 Mode-4 skip + 016 + 017 + Mode-3 + 018 Mode-4 + 019/020/021 = 7 non-extension including 2 skip-iters). Arms the proposed reverse portfolio-drift trigger from Item 4.
- **Recommendations:**
  1. **Escalate guardrail 6 from "flag" to "require explicit user acknowledgement"** for Mode 5 sequences of ≥3 items in a single Area. Acknowledgement must be recorded in the opening iteration's log block. This is a small policy change consistent with "reality before opinion" — make the risk unignorable.
  2. **Add Mode 5 companion-burn-down rule:** if a Mode 5 sequence of ≥3 items is proposed while pool > 8, the sequence MUST include at least 1 burn-down iteration OR be preceded by 1 burn-down iteration. For Path B, this means either inserting a burn-down between iter 018 (MR-004) and iter 019, OR making one of iter 019–021 a burn-down pick within the same Area. Path B as currently described (4× redesign) violates this — coordinator should insert a #14 or #15 burn-down iteration before iter 019.
  3. **Narrow cool-off clause 7 to exclude `directed`** (see Item 6). This prevents the next cool-off from being consumed the same way.
  4. **Do NOT shorten MR cadence** — MR-005 at iter 022 is correct. Shortening would change two control variables in one window.

## Agenda Item 10 — Open questions / new signals

- **Signal A — Closure ratio stagnation:** `IMPROVEMENT_BACKLOG.md` L55 shows 0.167 post-Mode-3 (down from 0.188). MR-003 revised target to 0.25 by iter 018. Current trajectory is 0.167 at iter 018-boundary → target missed. Root cause: pricing-audit intake added 7 rows (4 P0 + 3 M3) on a denominator that closed only 1 row (#33). Audit-intake pattern is functioning as designed, but KPI was set pre-intake. Recommend **revise target again to ≥0.25 by iter 025**, with visible tracking in `SYSTEM_HEALTH.md`.
- **Signal B — Scoring formula has dead axes:** See Item 8. `release_blocker_bonus` and `saturation_penalty` contribute zero discrimination in the current window. The formula is effectively 6 dimensions, not 8.
- **Signal C — PRD-artifact-as-scoring-input is emerging:** BUG-07's promotion on PRD-approval is the first case where a downstream artifact (a PRD) has modified backlog state. This is a healthy direction — strategic artifacts should influence execution priority — but it is currently ad-hoc. Item 2's proposed rule formalizes it.
- **Signal D — Mode 3 audit discovered 11 bugs on a surface with 0 prior loop visits:** The pricing/billing surface received **0** bounded-loop attention iter 001–015 (first web-app touch was iter 016), then Mode 3 immediately uncovered 3 P0 revenue-integrity bugs. This is exactly the Signal-5 risk MR-003 flagged, *confirmed retroactively*. Lesson for MR-005: "long portfolio drought on a surface" is a predictor of accumulated undetected P0s. Consider whether the reverse portfolio-drift trigger (Item 4) should carry a stronger response than "force a meta-review" — perhaps "force an audit-style 5-agent inspection of the under-surveyed surface" analogous to what Mode 3 did reactively.

## Proposed CLAUDE.md changes

All diffs minimal; each addresses one problem with one policy change (stability-window discipline).

1. **Narrow cool-off clause 7 to exclude `directed`.** (Change B of MR-004)
   - Current wording: `CLAUDE.md` § Follow-Up Debt Policy clause 7 "authorized to ignore clause 6 *once* and select by `top-score`, `blocker-cadence`, or `directed`."
   - Proposed wording: remove `, or \`directed\``. Rationale: `directed` items under Mode 2/5 already bypass clause 6 by operating-mode precedence; including `directed` in clause 7 consumes a single-use resource with zero formula-validation return. Observed at iter 016.
   - Evidence: Agenda Item 6.

2. **Add Mode 5 companion-burn-down rule.** (Change A of MR-004 — mandatory)
   - New clause in `CLAUDE.md` § Operating Modes, Mode 5 guardrails, clause 8: *"If the proposed Mode 5 sequence contains ≥3 items and the open follow-up pool exceeds 8, at least one iteration of the sequence MUST be a `burn-down` selection, OR the sequence MUST be preceded by one `burn-down` iteration. The burn-down iteration must target a follow-up whose Area overlaps the sequence's Area to preserve context locality."*
   - Rationale: Agenda Item 5 + Item 9. Prevents ceiling-rule inoperability over multi-item directed sequences.

3. **Escalate guardrail 6 to require explicit user acknowledgement.** (Change C of MR-004)
   - Current text: `CLAUDE.md` § Operating Modes, Mode 5 guardrail 6: "the coordinator must flag saturation risk to the user before beginning."
   - Proposed text: *"…must flag saturation risk to the user and receive explicit acknowledgement (captured in the opening iteration's `Candidate Selection` block as `mode-5-saturation: user-ack; rationale: [reason]`) before beginning."*
   - Rationale: Agenda Item 9 risk #1.

4. **Codify audit-intake + PRD-trigger promotion patterns.** (Change D of MR-004)
   - Add new `CLAUDE.md` § Audit-Intake Pattern (short, ≤20 lines):
     - P0-only live promotion; P1/P2/P3 stay in the audit artifact as cold pool.
     - Cold-pool rows tagged `audit-intake` with Birth iter = audit-producing iteration.
     - Promotion path 1: P0 burn-down creates slot for next-highest cold-pool item.
     - Promotion path 2: newly-approved PRD cites the item in an enumerated Dependencies section — then it promotes with Birth iter = `PRD-promoted` + PRD path reference.
     - No other promotion paths permitted.
   - Rationale: Agenda Items 1 + 2.

5. **Add reverse portfolio-drift early-trigger.** (Change E of MR-004)
   - New bullet in `CLAUDE.md` § Meta-Review Cadence early-triggers list: *"10+ consecutive iterations without touching any tracked extension surface (extension-app, segmentation-engine, normalization-engine, policy-engine) — flags reverse portfolio drift."*
   - Rationale: Agenda Items 4 + 9 + Signal D.

6. **Rule on test-only surface coverage:** test-file touches DO count toward portfolio-drift surface coverage for MR-003 Change D and the proposed Change E above. Add as a parenthetical note to the relevant bullets. Rationale: Agenda Item 4.

## Proposed scoring refinements

**Deferred to MR-005** to honor one-control-variable-at-a-time stability discipline. MR-004 is already introducing 5 governance diffs; adding scoring-term changes in the same window destroys effectiveness measurability. Seed the MR-005 agenda with: *"Evaluate whether to adopt `PRD-bonus +2` (Phase-2 alignment term) and `staleness-bonus +1` (follow-up age ≥10 term). Scoring is under-powered per MR-004 Agenda 8: 12 items within 2 points of live top."*

## Proposed agenda for MR-005

1. Did Change A (Mode 5 companion-burn-down) fire during Path B? Did it hold the pool?
2. Did the narrowed cool-off (Change B) produce a `top-score` discriminating selection when it next fires?
3. Scoring-formula under-power: adopt PRD-bonus + staleness-bonus or not?
4. Reverse portfolio-drift trigger status — armed? fired? what was the response?
5. Web-app over-correction — did Path B balance or over-shoot? Has any extension surface been touched since iter 014?
6. Staleness-cap re-scan: #7 (projected age ≥14), #14/#15 (if still open), any new entries.
7. Closure-ratio trajectory: is the Mode-5 window accounted for in the denominator correctly?
8. Audit-intake pattern — any P1/P2 cold-pool item that in hindsight should have been P0?
9. PRD-promotion pattern — did the formalization hold, or did coordinator discretion expand it?
10. Cool-off re-arming: how many consecutive burn-downs accumulated post-Path B?

## Cadence note

- **MR-004 completed at iter 018.** Stability window runs through iter 021 (3 loops, per MR-001 floor rule).
- **MR-005 earliest:** iter 022 boundary. Path B iter 019–021 increments Mode-5 counter by 3 per `CLAUDE.md` guardrail 1; combined with MR-004 consuming iter 018, base cadence (3 bounded loops) lands at iter 022.
- **Early-trigger watch during Path B (iter 019–021):**
  - Same-implementer-4+ if Path B lands all 4 on one primary.
  - 3-consecutive-same-Area if Path B is uniformly web-app (expected — the proposed user-ack guardrail escalation is the primary mitigation).
  - Reverse portfolio-drift (if adopted) will be armed but not yet fired.
  - Follow-up pool > 10 (already true, ongoing).
  - Validation-failure-2-in-a-row: no signal so far; watch.
