{
  "name": "ledgerium",
  "private": true,
  "version": "0.0.1",
  "description": "Ledgerium AI — Universal Work Ledger monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm --filter extension-app dev"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
